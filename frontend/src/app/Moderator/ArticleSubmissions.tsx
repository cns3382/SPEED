import { title } from 'process';
import './style.css'

interface ArticleSubmissionsProps {
    title: string;
    authors: string;
    pubyear: string
};

const ArticleSubmissions = (props: ArticleSubmissionsProps) => {
    return(
        <div className='all-submissions-block'>
            
        </div>
    );
}